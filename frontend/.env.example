# Copy to .env.local for local overrides.
# Defaults to a relative "/api" path which works with the Vite dev proxy and
# with CloudFront's /api behavior in production. Override only if needed.
# VITE_API_BASE=/api
